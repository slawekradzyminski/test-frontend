/// <reference types="cypress" />

describe('Home page with mocks', () => {

    beforeEach(() => {
        cy.setTokenInLocalStorage()
        cy.visit('')
    })
  
    it('should delete user', () => {
    })

  })
  